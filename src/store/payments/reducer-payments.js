// reducers and projections (derived state calc in views)
// for payments

// transform function from api result to store
export const paymentTransform = (data) => data;

// config for filters
const paymentFilters = {};

